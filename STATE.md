# STATE.md — website (nalbana.com)
*Volatile. Seeded by the 2026-07-08 audit.*

**Last touched:** 2026-08-16 — **⟳ Refresh button on owner.html (pending commit).** Armen: "add a calendar refresh button to the owner calendar webpage and make sure it's still visible in mobile." Note the two owner calendars: `Nalbana_FPA/docs/owner.html` (ops.nalbana.com) has had one since 2026-08-09 — THIS page (nalbana.com/owner.html, the one Armen actually uses) never did. Implementation: `<span id="refresh-slot">` inside a new `.header-actions` flex wrapper, left of the Offer Generator CTA; `NalbanaRerun.mount()` from `https://ops.nalbana.com/rerun.js` — the same cross-site script contract as `cleaning_classify.js`, so no code is duplicated into this repo — dispatching `booking_refresh.yml` with `onDone: init` so the calendar, cleaning diamonds and the "Rates:" stamp all re-render when the run finishes. Mount is guarded on `window.NalbanaRerun`: rerun.js is fetched cross-site and a failed load must not take the calendar down. Mobile (≤600px): `.header-actions` wraps, and both buttons tighten to `9px 13px / 10px / 0.12em` so they sit side by side at 390px instead of one wrapping alone; added a `:disabled` style since rerun.js disables the button while the run is in flight. **Depends on the same-day Nalbana_FPA change** that added the PriceLabs fetch to `booking_refresh.yml` — before it, this button would have refreshed bookings and cleanings but NOT the rates this page renders. Push Nalbana_FPA first. Verified read-only: inline JS passes `node --check`, 10/10 structural checks (slot exists, CSS defined, mount after `init` is defined, guard present, offer button intact). **NOT exercised in a browser** — after deploy confirm on a phone that both buttons are visible and that a press flips the label and reloads the rates stamp. **Same session — property thumbnails now link to their Airbnb listings** (Armen: "best place for me to grab links to text/send on airbnb to people, which i do often"). `PROPERTIES` gains an `airbnb` field via an `AIRBNB(id)` helper; the thumb is wrapped in `a.prop-thumb-link` (`target=_blank`, `rel="noopener noreferrer"`, title hinting right-click-to-copy). Listing IDs were NOT guessed — cross-checked against two independent existing sources that agree on all six: the `airbnb.com/rooms/<id>` links already on this site's own property pages + `index.html`, and `Nalbana_FPA/agents/fp-and-a/listing_map.json`. CSS: `display:block; line-height:0` so the anchor adds no descender gap in the sticky column, hover tint only (a border or scale would shift the column). Verified 7/7 structural + `node --check`; not yet clicked in a browser. Security note: the page is behind the client-side password wall, but that wall is bypassable by anyone reading the source, and `fpa-proxy` attaches the PAT server-side — so this button is dispatch-capable for anyone who gets the page. The proxy was already publicly callable, so this adds no new exposure, but it's the reason to keep ops controls off the un-gated pages. Prior: 2026-07-21 (round 2) — polish after Armen's live review: time indicator rebuilt as ONE continuous overlay line (was per-cell, looked broken behind row borders), today-column side borders removed (tint only — read as three bars), live ET clock under the TODAY header tag, "Today's events" IN/OUT chips in the top-left corner dead space (calendar.html only). **DEPLOYED** (Armen confirmed push 2026-07-21). HTTPS fully healed: cert re-provisioned, Enforce HTTPS ticked, http→https 301 verified live. Round 1 (ET-pinning + today overhaul, both repos) also **DEPLOYED**.

## What's broken / open
- ~~HTTPS broken~~ **RESOLVED 2026-07-21**: cause was a wedged GitHub Pages cert renewal (the "blank calendar" — Chrome HTTPS-first hit the dead endpoint). Fixed by custom-domain remove/re-add (retriggers Let's Encrypt) + Enforce HTTPS ticked; http→https 301 verified. If it ever wedges again: same dance, or move nalbana.com behind the Cloudflare proxy (orange cloud + Flexible SSL) for permanently self-healing edge certs — decided 2026-07-21 not to pre-build automation for this.
- Cross-repo dependency on ops.nalbana.com JSONs was undocumented until this audit — now a named contract (see README). Long-term cleanup: MIGRATION_PLAN step 11 (retire duplicate ICAL secrets).
- ICAL_* secrets duplicated with Nalbana_FPA repo — iCal URL rotation must be done in both repos until step 11.

## What's next
1. **Remaining verification:** eyeball the round-2 polish live (continuous line, ET clock, events chips), and test ET pinning from a non-ET timezone (temporarily change Mac timezone — grid/"today" must not shift). HTTPS already verified.
2. Portfolio-website improvements remain roadmap priority 4 (after guest diligence, FP&A, marketing).

## Session log
- 2026-07-21 (round 3) — Second live review, calendar.html + owner.html: (1) time line now starts below `thead` (`line.style.top = thead.offsetHeight`) so it never covers the TODAY/date header text; (2) in-header 7px clock replaced by floating `#et-time-label` ("7:54 AM ET", 13px Georgia gold, `translateX(-50%)` at the line's x, top of grid, free to span column widths, z7 under sticky labels); (3) today's-events chips ported to owner.html (compact corner variant, reads existing `_bookingsCache` global, prop short names from `p.name`). Both pages re-verified: node --check, no leftover `today-time` refs, ids/classes defined.
- 2026-07-21 (round 2) — Live-review tweaks, calendar.html + owner.html: (1) `#et-time-line` is now a single absolutely-positioned overlay div in `#cal-container` (position:relative), placed at `todayTH.offsetLeft + frac·38px` by `updateEtClock()` — z-index 6 (above pills z1 + diamonds z5, below sticky prop labels z10 so it tucks under when scrolling); replaced the per-cell `::after` approach that rendered behind row borders. (2) `.today-col`/`.today-head` side borders removed — background tint only (three-bars confusion). (3) Live ET clock (`#today-time`, "3:42 PM ET") under the date in the TODAY header, refreshed every 60 s. (4) calendar.html only: "Today's events" chips in the corner dead space (`.today-events` in the day-row corner th) — per property with activity today: OUT/IN chips, guest name on hover, nothing printed when no activity; renderCalendar now takes `bookings` as 5th param. Verified: node --check both pages, class/id sweep, null-guards on all new getElementById targets. Owner.html can get the events box later by porting the corner2 block (its corner is 100px wide — needs a narrower layout).
- 2026-07-21 — ET-pinning + today-column overhaul + blank-page triage (the queued item 1 spec, now done code-side). **Triage:** blank calendar was NOT a JS/data bug — live bookings.json (incl. the new `canceled`/`canceled_at` records) passes cleanly through buildLookup (verified offline against live data). Root cause is broken HTTPS on nalbana.com (see What's broken — Armen has the fix steps; DNS already verified correct). **Build (all three surfaces — calendar.html + owner.html here, docs/owner.html in Nalbana_FPA):** "today"/past logic now reads the shared `NalbanaET` helper added to `ops.nalbana.com/cleaning_classify.js` (ONE ET source, device-time fallback if the script fails to load — same graceful degradation as the diamonds); header keeps the real day-of-week + date with a small gold TODAY tag above (previously 'Today' replaced the DOW); `.today-col`/`.today-head` hard highlight (stronger tint + gold left/right borders); new `::after` bar slides across the today column pro-rata to the 24h ET clock via `--et-frac`, refreshed every 60 s by `updateEtClock()` (no re-render); owner.html offer-form tomorrow-prefill also ET-pinned. Verified: node --check on all extracted inline JS + classifier, NalbanaET unit tests, classifier regression suite, buildLookup smoke test vs live booking shapes, CSS class/getElementById sweep. Push order: Nalbana_FPA first (cleaning_classify.js is the shared contract), then this repo.
- 2026-07-09 — Fixed owner.html cleaning diamonds: page never fetched `cleaning_schedule.json`, so its stale `cleaningDiamond()` fork showed red ◇ "NOT scheduled" for cleanings that WERE on the cleaner's calendar (smoke test: 14 of 15 upcoming cleanings were false-red). owner.html now fetches the schedule, applies suppression + property whitelist (Jemez excluded, matching calendar.html), and both owner.html and calendar.html consume the shared classifier `ops.nalbana.com/cleaning_classify.js` (new 4th contract file; classification shared, site palette local; pages skip diamonds gracefully if the script fails to load). Full diagnosis: `Nalbana_FPA/docs_meta/STATE.md` session log.
- 2026-07-08 — Audit: added README/STATE; no site changes.
